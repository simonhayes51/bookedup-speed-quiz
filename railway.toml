[service]
name = "bookedup-speed-quiz"
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[variables]
SECRET_KEY = "change-me"
# Option A seeding (remove after first login):
# ADMIN_EMAIL = "you@domain.com"
# ADMIN_PASSWORD = "SuperSecure123!"
# ADMIN_NAME = "Your Name"
# Option B bootstrap (remove after bootstrapping):
# BOOTSTRAP_TOKEN = "generate-a-strong-token"
